import { LoginForm } from '@/components/auth/LoginForm'

export default function LoginPage() {
  return <LoginForm />
}

export const metadata = {
  title: 'Sign In - Clicksi',
  description: 'Sign in to your Clicksi account',
}